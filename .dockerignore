# Keep the build context small and never ship local state / secrets.
node_modules
.next
.git
.github
coverage
npm-debug.log*

# Local env & secrets — provided at runtime by Cloud Run, never baked in.
.env
.env.*
!.env.example

# Editor / OS / tooling
.vscode
.idea
.DS_Store
.claude

# Docs & tests aren't needed in the runtime image.
*.md
docs
**/*.test.ts
**/*.test.tsx

# ...but brand/tasks/*.md and the Mink prompts ARE read at runtime (and
# traced into .next/standalone via outputFileTracingIncludes). Keep only those
# prompt sources in the context.
!brand
!brand/tasks
!brand/tasks/*.md
!docs
docs/*
!docs/mink-ai-system-prompt.md
!docs/mink-ai-image-prompt.md
